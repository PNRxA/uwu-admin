# E2E test configuration
# Copy to .env and fill in values

# Base URL for E2E tests (set to quadlet container or omit to use Vite dev server)
# E2E_BASE_URL=http://127.0.0.1:8080

# Required for E2E tests — Matrix homeserver connection
TEST_HOMESERVER=
TEST_USERNAME=
TEST_PASSWORD=
TEST_ROOM_ID=
